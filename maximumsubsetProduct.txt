#include bitsstdc++.h
using namespace std;

int maxProductSubset(int a[],int n)
{
sort(a,a+n);
int product = 1;
int value = 1;
int last;

for(int i=0;in;i++)
{
    if(a[i]0)
 product = producta[i];
 if(a[i]0)
 {
     value = valuea[i];
     last = a[i];
 }
 
}
if(value0)
return (valueproduct);
else
{
    value = valuelast;
    return (valueproduct);
}
}

int main()
{
    int a[] = {  -1, -1, -2, 4, 3  };
    int n = sizeof(a)  sizeof(a[0]);
    cout  maxProductSubset(a, n);
    return 0;
}